/*
.By default javascript is a synchronous 
      means execute code in a sequence manner
.javascript is a single threaded
      means execute code on single thread
      console.log(1);
      console.log(2);
      firstly execute 1 and the 2 this is a single thread in call stack

.Blocking code                vs                   non blocking code
   block the flow of program                 does not block execution
   read file sync                             read file async


.task queue    and promoisses or high priority queue
bsicaly task queqe make js async more afficiant


.fetch() api make js code faster compare to other in this promisses queue is involve
 */