/*
.............------javascript execution context------------
     . JEC means hhow do javascript run or execute the code file
.Their are three EC step
     .1  global EC (it refer to this key and return windows object)
     .2  function EC
     .3 eval EC (not need to study because it is also a global object)
.javascript run the code in two phase
     .1 memory creation phase (some time called creation phase  in this phase memory is allocated to the variable )
     .execution phase (all thhe logic is done in thhis phase)
 */